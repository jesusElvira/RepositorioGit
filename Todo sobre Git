user: jesus
email: jesuselvirasanchez@gmail.comç

comandos://////////////////////////////////////

git add -> añadimos el archivo al index.

git commit -> añadimos la version del fichero que queremos introducir a nuestro repositorio. /// -m(sirve para añadir comentarios al comit). ej commit -m "Comentarios del archivo".

git log -> muestra quien ha hecho el commit. (cada comit es identificado con un identificador unico).

git remote add remoto https://github.com/jesusElvira/RepositorioGit.git
-> añadimos un repositorio remoto en el cual queremos subir nuestros archivos.

git remote -v  -> nos enseña todos los directorios remotos sobre los que podemos trabajar.

git push remoto master -> empujar el commit al repositirio.(nota: remoto es el nombre que le he dado desde consola al repositotio y master es la rama).

git clone https://github.com/jesusElvira/ZeroC-Ice-HolaMundo..git -> crea una copia de un repositorio de git hub en el directorio actual.

git config --global user.name "jesus" -> configurar el nombre de usuario.
